// Código compartido entre la web (apps/web) y la app móvil (apps/mobile):
// tipos, reglas de negocio puras (por ejemplo, el reloj contractual o la
// capa central de permisos) y utilidades sin dependencias de React ni de
// Next.js/Expo. Todavía vacío en el Hito 1 — se empieza a llenar en el
// Hito 2 con la capa de permisos (ver docs/PLAN-H1-H2.md).

export const CUOTLY_SHARED_PACKAGE_READY = true;
